const Loader = () => {
  return (
    <div
      style={{
        height: "80%",
        display: "grid",
        placeItems: "center",
      }}>
      <h1>Loading...</h1>
    </div>
  );
};

export default Loader;
