const Loader = () => {
  return (
    <div
      style={{
        height: "100%",
        display: "grid",
        placeItems: "center",
      }}>
      <h1>Loading...</h1>
    </div>
  );
};

export default Loader;
